{
  "name": "ladrillosjs-benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "prebench": "npm --prefix .. run build",
    "bench": "node run.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.50.0"
  }
}